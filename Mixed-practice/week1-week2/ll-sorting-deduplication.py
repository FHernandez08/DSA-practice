# Given a single linked list of integers that may contain duplicates, perform the following:
#   Sort the linked list using Quick Sort
#   Remove duplicate elements so that each number appears ONLY once.